# 财记

财记是一个本地优先的私人资金管理网页应用。运行时使用原生 HTML、CSS 和 JavaScript，数据默认保存在浏览器 `localStorage`。

## 开始使用

- 普通本地功能：直接打开 `index.html`。
- PWA、Service Worker 和缓存验证：通过 localhost HTTP 服务打开仓库根目录。
- 完整工程检查：`node scripts/check-project.js`。

项目现状、数据边界和维护约束见 [项目文档索引](docs/README.md)。当前代码的本地验证不代表已经部署或线上可用。
